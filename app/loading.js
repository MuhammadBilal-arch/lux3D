export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return (
    <div className="min-h-screen bg-blue-gradient flex items-center justify-center text-white text-center">
      <div>Loading...</div>
    </div>
  );
}
